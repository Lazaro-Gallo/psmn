<?php
$request = $this->request();
$controller = $request->getControllerName();
$action = $request->getActionName();

if (Zend_Auth::getInstance()->hasIdentity()) {
    include_once 'new-qstn-logged.phtml';
    return;
}

include_once 'new-qstn.phtml';